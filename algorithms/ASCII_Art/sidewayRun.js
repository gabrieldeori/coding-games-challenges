const L = parseInt(readline()); // Largura da letra
const H = parseInt(readline()); // Altura da letra
const T = readline(); // Texto a ser escrito em ASCII
for (let i = 0; i < H; i++) {
    const ROW = readline(); // Uma string enorme com uma fonte pré-fabricada
}

console.log('answer');
